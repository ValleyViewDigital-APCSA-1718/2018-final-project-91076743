import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class AllGhost here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public interface AllGhost extends Actor
{
    /**
     * Act - do whatever the AllGhost wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act(); 
    
        
       
    public void respawn();
    
      //
    
}
